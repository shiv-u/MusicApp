<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidexample.musicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="RadioGaga"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".classicActivity"
            android:label="Classic"


    />
        <activity
            android:name=".rockActivity"
            android:label="RockandRoll"

            />
        <activity
            android:name=".popActivity"
            android:label="Pop"
           />
        <activity
            android:name=".hipActivity"
            android:label="HipHop"
         />
        <activity android:name=".play"
            android:label="Play"
            android:parentActivityName="com.androidexample.musicapp.MainActivity"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.androidexample.musicapp.MainActivity" />
        </activity>
    </application>

</manifest>